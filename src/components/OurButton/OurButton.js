import { Button } from 'react-bootstrap';

const OurButton = () => {
  return (
    <div>
      <Button variant="outline-light" size="lg">
        Discover
      </Button>
    </div>
  );
};

export default OurButton;
